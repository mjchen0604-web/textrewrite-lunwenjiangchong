services:
  textrewrite-mcp:
    build: .
    image: textrewrite-remote-mcp:1.0.0
    container_name: textrewrite-mcp
    environment:
      HOST: 0.0.0.0
      PORT: 8793
    ports:
      - "127.0.0.1:8793:8793"
    restart: unless-stopped
